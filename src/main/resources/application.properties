spring.application.name=tourconnect-backend

# SERVER PORT (Render)
server.port=${PORT:8080}

# DATABASE (Railway via Render env variables)
spring.datasource.url=jdbc:mysql://switchback.proxy.rlwy.net:57257/railway
spring.datasource.username=root
spring.datasource.password=EbFmLYhZSXdsqgufbYqZRoERNDUEQdmv

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# MAIL (use env variables)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER}
spring.mail.password=${MAIL_PASS}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# GOOGLE LOGIN (use env variables)
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email